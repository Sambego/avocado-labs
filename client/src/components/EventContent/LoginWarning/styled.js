import styled from 'styled-components'

export const LoginWarningStyled = styled.p `
  margin-top: 4rem;
  font-size: 1.6rem;
  line-height: 150%;
  font-style: italic;
  color: #ffffff;

  a {
    cursor: pointer;
    color: #000;
    border-bottom: 1px solid #000;
  }
`